# Sanity Configuration (Frontend - Read Only)
VITE_SANITY_PROJECT_ID=your_sanity_project_id
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01

# Server-side Sanity API Token (Required for API endpoints)
# This should be a token with write permissions for contact form submissions
SANITY_API_TOKEN=your_sanity_write_token

# Optional: Custom domain for production
VERCEL_URL=your-domain.com

# Development Notes:
# - Get your project ID from Sanity dashboard
# - Create an API token with write permissions for server-side operations
# - Use 'production' dataset for live data, 'development' for testing